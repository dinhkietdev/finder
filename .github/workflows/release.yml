name: Release Finder

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'desk/**'
      - '.github/workflows/release.yml'

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-latest
    defaults:
      run:
        working-directory: desk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: desk/package-lock.json
      - run: npm ci
      - name: Configure Firebase Authentication
        run: |
          node -e "require('fs').writeFileSync('firebase-auth-config.js', 'module.exports = { apiKey: ' + JSON.stringify(process.env.FIREBASE_WEB_API_KEY) + ' };\n')"
        env:
          FIREBASE_WEB_API_KEY: ${{ secrets.FIREBASE_WEB_API_KEY }}
      - name: Set release version
        run: node -e "const fs=require('fs'); const p=require('./package.json'); p.version='1.0.${{ github.run_number }}'; fs.writeFileSync('package.json', JSON.stringify(p, null, 2) + '\n');"
      - name: Create release tag
        run: |
          TAG="v1.0.${{ github.run_number }}"
          if ! git ls-remote --exit-code --tags origin "refs/tags/$TAG" > /dev/null 2>&1; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git tag -a "$TAG" -m "Finder $TAG"
            git push origin "$TAG"
          fi
      # Package first, then upload with the GitHub CLI.  electron-builder
      # uploads the artifact and its blockmap concurrently, which can leave a
      # release with only the first asset.
      - name: Build macOS installers
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --mac --publish never
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: v1.0.${{ github.run_number }}
        run: |
          cat > release-notes.md <<'EOF'
          ## Cài đặt macOS

          1. Mở file **Finder-*.dmg** và kéo Finder vào thư mục **Applications**.
          2. Nếu macOS chặn app, hãy nhấp Control + click vào Finder.app, chọn **Open**, rồi xác nhận **Open**.
          3. Nếu vẫn hiện thông báo *Finder.app bị hỏng*, mở Terminal và chạy:

          ```bash
          xattr -cr /Applications/Finder.app
          ```

          Chỉ thực hiện bước 3 khi bạn tải Finder từ release chính thức này. Bản macOS hiện chưa được Apple notarize.
          EOF
          gh release create "$TAG" --title "Finder $TAG" --notes-file release-notes.md || true
      - name: Upload macOS release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: v1.0.${{ github.run_number }}
        run: gh release upload "$TAG" dist/*.zip dist/*.dmg dist/*.blockmap dist/latest-mac.yml --clobber

  build-windows:
    needs: build-macos
    runs-on: windows-latest
    defaults:
      run:
        working-directory: desk
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: desk/package-lock.json
      - run: npm ci
      - name: Configure Firebase Authentication
        run: |
          node -e "require('fs').writeFileSync('firebase-auth-config.js', 'module.exports = { apiKey: ' + JSON.stringify(process.env.FIREBASE_WEB_API_KEY) + ' };\n')"
        env:
          FIREBASE_WEB_API_KEY: ${{ secrets.FIREBASE_WEB_API_KEY }}
      - name: Set release version
        run: node -e "const fs=require('fs'); const p=require('./package.json'); p.version='1.0.${{ github.run_number }}'; fs.writeFileSync('package.json', JSON.stringify(p, null, 2) + '\n');"
      # Build Windows locally, then upload to the release created by macOS.
      # electron-builder cannot publish to an existing non-draft release.
      - name: Build and publish Windows installer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx electron-builder --win --publish never
      - name: Upload Windows release assets
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: v1.0.${{ github.run_number }}
        run: gh release upload "$TAG" dist/*.exe dist/*.blockmap dist/latest.yml --clobber
